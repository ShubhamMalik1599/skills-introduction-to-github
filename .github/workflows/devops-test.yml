name: ServiceNow DevOps Test

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build
        run: echo "Build started"

      - name: Test
        run: echo "Running tests"

      - name: Deploy
        run: echo "Deploying application"
